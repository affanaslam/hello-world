#ifndef IMAGE_H
#define IMAGE_H
#include <string>

using namespace std;

int halve(string fileName);

int threshold(string fileName, int t_val);

int strToint(string num);

int doubleUp(string fileName);

#endif 